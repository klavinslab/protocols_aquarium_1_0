place one
  protocol: "one.pl"
  group: "technician"
  marked: true
  start: now()
  window: minutes(5)
end

place two
  protocol: "two.pl"
  group: "technician"
  start: now()
  window: minutes(5)
end

transition [one] => [two] when !error(0) && completed(0) end
transition [two] => [] when !error(0) && completed(0) end

