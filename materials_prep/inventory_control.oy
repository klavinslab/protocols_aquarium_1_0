place pour_yeast_plates
  protocol: "materials_prep/reagents/make_1kb_ladder_aliquot.pl"
  group: aquarium_user
  marked: true
  start: now()
  window: minutes(30)
end

transition [make_1kb_ladder_aliquot] => [make_1kb_ladder_aliquot] when error(0) || quantity('SDO -His Plate (sterile)') < 20 end
