argument
  x: generic
end

place p1
  protocol: "oyster/arbitrary.pl"
  group: aquarium_user
  marked: true
  argument
    x: x
  end
  start: now()
  window: hours(1)
end

transition [ p1 ] => [ ] when !error(0) && completed(0) end